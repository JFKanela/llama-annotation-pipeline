name: llama_gfftools
channels: [conda-forge, bioconda]
dependencies:
  - gffread=0.12.7
  # Version pinned a posteriori to match the version actually used; see
  # REPRODUCIBILITY.md, "Exact tool versions".
  - gffcompare=0.12.10
